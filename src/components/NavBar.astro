<header class="header" id="Navbar">
    <div class="logo">
        <img src="../public/logoblanco.png" alt="">
    </div>
    <nav class="nav">
        <ul class="nav-links">
            <li><a href="/">Inicio</a></li>
            <li><a href="/servicios">Servicios</a></li>
            <li><a href="/contactanos">Contactanos</a></li>
            <li><a href="/videos">Videos</a></li>
        </ul>
    </nav>

    <div class="menu-toggle" id="menu-toggle">
        <span></span>
        <span></span>
        <span></span>
    </div>
</header>

<style>
    @import url('https://fonts.googleapis.com/css2?family=Nunito+Sans:ital,opsz,wght@0,6..12,200..1000;1,6..12,200..1000&display=swap');
    

    .header{
        position:fixed;
        display: flex;
        justify-content: space-between;
        align-items: center;
        width: 100%;
        height: 85px;
        font-family: 'Helvetica', 'Arial', sans-serif;
        font-size: 20px;
        padding: 5px 5%;
        transition: top 0.8s ease, opacity 0.8s ease;
        background: #6c02e3;
        box-shadow: 0 5px 10px 0 rgb(0, 0, 0, 0.6);
        z-index: 111;
        opacity: 1;
}

/* Nueva clase para ocultar */
.header.hide {
    top: -70px;
    opacity: 0;
    pointer-events: none; /* opcional: evita clics mientras está oculto */
}

    .header .logo{
        cursor: pointer;
    }

    .header .logo img{
        height: 70px;
        width: auto;
        transition: all .3s ;
    }

    .header .logo img:hover{
        transform: scale(1.2);
    }

    .header .nav-links{
        list-style: none;
    }

    .header .nav-links li{
        display: inline-block;
        padding: 0 20px;
        transition: all .3s ease 0s;
    }


    .header .nav-links li:hover{
        transform: scale(1.1);
    }

    .header .nav-links a{
        font-size: 700;
        text-decoration: none;
        color: white;
    }

    .header .nav-links li a:hover{
        border-radius: 10px;
    }


    .menu-toggle {
        width: 40px;
        height: 25px;
        display: none;
        flex-direction: column;
        justify-content: space-between;
        cursor: pointer;
        z-index: 1001;
    }

    .menu-toggle span {
        display: block;
        height: 3px;
        margin: 4px;
        background: #ffff;
        border-radius: 3px;
        transition: 0.4s;
    }

    .menu-toggle.active span:nth-child(1) {
    transform: rotate(45deg) translate(5px, 5px);
    }

    .menu-toggle.active span:nth-child(2) {
    opacity: 0;
    }

    .menu-toggle.active span:nth-child(3) {
    transform: rotate(-45deg) translate(5px, -5px);
    }
    @media (max-width: 768px) {
        .nav {
            position: absolute;
            top: 85px;
            right: 0;
            width: 100%;
            background-color: rgb(0, 0, 0, 0.9);
            flex-direction: column;
            transform: translateX(-100%); /* Oculto fuera de la pantalla */
            transition: transform 0.3s ease-in-out;
            z-index: 1000;
        }
        .header .nav-links li{
            width: 100%;
            padding: 15px 35px;
            border-bottom: 2px solid white;
        }
        .header .nav-links a{
        color: white;
        }

        .nav.active {
            transform: translateX(0);
        }

        .menu-toggle {
            display: block;
        }
    }
</style>

<script>
   
</script>